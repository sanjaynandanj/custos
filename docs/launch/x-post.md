# X / Twitter — Custos v0.3.0

Two variants. Pick one.

## A — Technical

Custos v0.3.0 is out.

Policy Enforcement Point + native PDP + Ed25519 hash-chained audit ledger for AI agent tool calls. Works with MCP, LangGraph, Claude Agent SDK.

Python + Node, wire-compatible. Deny-by-default. Offline-verifiable.

github.com/sanjaynandanj/custos

## B — Accessible

Stop your AI agent from rm -rf'ing your prod DB.

Custos is a deny-by-default policy layer + signed audit log for every tool call an agent makes. Wraps MCP servers, LangGraph tools, Claude Agent SDK tools.

Open source, v0.3.0 out today.

github.com/sanjaynandanj/custos
