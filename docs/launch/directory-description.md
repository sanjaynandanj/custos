# Third-party directory blurbs

For MCP registries, awesome-lists, and similar indexes.

## Long (max 300 chars — 249)

Custos is a Policy Enforcement Point and cryptographic audit ledger for AI agent tool calls. Wraps MCP servers via stdio proxy or gates any tool via in-process SDK. Ed25519-signed hash-chained JSONL, offline-verifiable. Adapters for LangGraph and Claude Agent SDK. Python + Node, Apache-2.0.

## Short (<100 chars — 91)

Policy enforcement + signed audit ledger for AI agent tool calls. MCP + LangGraph + Claude SDK.

## Tag-line (<60 chars — 54)

Deny-by-default policy + signed audit log for AI agents.
