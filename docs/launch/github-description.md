# GitHub repo description

Meta description shown at the top of the repo page. Max 200 chars.

## Primary (196 chars)

Cryptographic audit ledger and policy enforcement layer for AI agent tool calls. Works with MCP, LangGraph, and the Claude Agent SDK. Python + Node, wire-compatible. Apache-2.0.

## Alternate (177 chars)

Policy enforcement + tamper-evident audit ledger for AI agents. MCP-compatible, framework-agnostic (LangGraph, Claude Agent SDK). Ed25519-signed hash chain. Apache-2.0.

## Website field

https://github.com/sanjaynandanj/custos

## Topics (paste into repo Topics)

`mcp`, `authorization`, `policy-as-code`, `ai-agents`, `agent-security`, `governance`, `audit-trail`, `ed25519`, `opa`, `cedar`, `langgraph`, `claude-agent-sdk`
