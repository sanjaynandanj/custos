# npm / PyPI package description

Single-line `description` field for `package.json` and `pyproject.toml`.

## Current (both packages, as of 0.2.x)

> Runtime governance, policy enforcement, and cryptographic audit ledger for MCP tool calls

## Proposed for v0.3.0

Reposition around what Custos actually is (PEP + audit ledger) and who it works with (not just MCP):

> Policy enforcement layer and cryptographic audit ledger for AI agent tool calls. MCP-compatible, framework-agnostic (LangGraph, Claude Agent SDK).

Character count: 168 — safe for npm (no hard limit but ~200 is convention) and PyPI (which shows the full string on the project page).

## Shorter alternate (for terser index formats)

> Policy enforcement + tamper-evident audit ledger for AI agent tool calls.
