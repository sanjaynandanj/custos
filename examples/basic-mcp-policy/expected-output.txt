read_file('/workspace/notes.txt') -> allow  reason='workspace-only reads'
read_file('/etc/passwd') -> deny  reason='default:deny'

ledger head: sha256:<64-hex-chars>   records: 2

# Notes:
# - The ledger head hash will differ every run (fresh keypair -> fresh chain).
# - Node output is the same but uses `newActor(...)` and JSON-stringified reason.
# - .custos/ledger.jsonl now contains 2 JSONL lines, one per decision.
